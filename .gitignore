# Env / secrets
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
frontend/dist*/
frontend/vite.config.js.timestamp-*.mjs

# Stray FUSE-mount build/lock leftovers, renamed here rather than deleted
# (see PROJECT-PLAN.md's FUSE operational note)
.trash/
backend/tests/test_prompt_len_tmp.py

# Data (persisted via volume, not committed)
data/*
!data/.gitkeep
!data/uploads
data/uploads/*
!data/uploads/.gitkeep

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
